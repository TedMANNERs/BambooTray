<Window x:Class="BambooTray.App.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:popup="clr-namespace:BambooTray.App.View.Popup"
        Visibility="Hidden" ShowInTaskbar="False" WindowStyle="None" 
        Width="0" Height="0" Icon="../Images/bamboo.ico" ShowActivated="False"
        Closed="Window_Closed" Loaded="Window_Loaded">

    <tb:TaskbarIcon Name="TaskbarIcon"
                    IconSource="{Binding PopupViewModel.IconSource}"
                    PopupActivation="LeftClick"
                    MenuActivation="RightClick"
                    ToolTipText="BambooTray">
        <tb:TaskbarIcon.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Login" Command="{Binding LoginCommand}"/>
                <MenuItem Header="Exit" Click="Exit_Click"/>
            </ContextMenu>
        </tb:TaskbarIcon.ContextMenu>
        <tb:TaskbarIcon.TrayPopup>
            <popup:PopupView DataContext="{Binding PopupViewModel}"/>
        </tb:TaskbarIcon.TrayPopup>
    </tb:TaskbarIcon>
</Window>
