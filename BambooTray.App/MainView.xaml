<Window x:Class="BambooTray.App.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BambooTray.App"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        Visibility="Hidden" ShowInTaskbar="False" WindowStyle="None" 
        Width="10" Height="10" Icon="Images/bamboo.ico"
        Closed="Window_Closed" Loaded="Window_Loaded">

    <tb:TaskbarIcon Name="TaskbarIcon"
                    IconSource="{Binding PopupViewModel.IconSource}"
                    PopupActivation="LeftClick"
                    MenuActivation="RightClick">
        <tb:TaskbarIcon.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Login" Command="{Binding LoginCommand}"/>
                <MenuItem Header="Exit" Click="Exit_Click"/>
            </ContextMenu>
        </tb:TaskbarIcon.ContextMenu>
        <tb:TaskbarIcon.TrayPopup>
            <local:PopupView DataContext="{Binding PopupViewModel}"/>
        </tb:TaskbarIcon.TrayPopup>
    </tb:TaskbarIcon>
</Window>
